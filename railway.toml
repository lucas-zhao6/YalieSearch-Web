[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"
watchPatterns = ["backend/**"]

[deploy]
# startCommand not needed - Dockerfile CMD is used instead
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
